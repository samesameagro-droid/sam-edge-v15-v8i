name: TEST SCHEDULE

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Scheduler test
        run: |
          echo "SCHEDULE WORKS"
          date -u
